<Page x:Class="kxdanmuji.Pages.SettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:kxdanmuji.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="SettingPage">

    <Grid Background="{DynamicResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" >
            <Button x:Name="btnBack" Content="弹幕姬设置" HorizontalAlignment="Right" Style="{DynamicResource BackButton}" Margin="0,0,0,0" FontSize="18" Click="btnBack_Click" />
        </StackPanel>
        <Border BorderThickness="0,0,0,1" Height="2" VerticalAlignment="Bottom" Style="{DynamicResource warpper}"/>
        <ScrollViewer Margin="0,0,0,0" VerticalAlignment="Top" Grid.Row="1"/>

    </Grid>
</Page>
